# HU14 – Modificar OT (Contrato con Proveedor)

Prototipo **TRL4** (datos simulados). Cubre:
- Seleccionar OT y editar campos (ID no editable).
- Validaciones: obligatorios y `fechaInicio ≤ fechaFin`.
- Regla: si la OT está **Finalizada**, solo **Admin** puede editar (simulado).
- Mensajes de éxito/error y auditoría simulada.

## Ejecutar

